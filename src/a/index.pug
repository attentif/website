---
title: Published stuff
---
ul.articles-list
  for a in articles
    li
      // chop extension off path as we use permalinks
      a(href=`/${helpers.trimFileExtension(a.path)}`)
        h2
          != a.title
      time(datetime=a.date)
        != helpers.formatDate(a.date)
      p
        != a.description
